<!DOCTYPE html>
<html lang="es">
<%- include('./partials/head') %>
<body>
<%- include('./partials/header') %>
<!-- product detail -->
<div class="container products-wrapper">
    <div class="d-flex align-items-center">
        <% for( let i = 0; product.length; i++ ) { %>
            <h2 class="products-title"><%= product.nombreProducto[i] %></h2> 
        <% } %>
        <p class="product-detail-descrption">
            <a href="/products/productsAdd">Editar</a>
        </p>
        <!-- mail del usuario -->

    </div>
    <div class="product-detail">
        <div class="row">
            <article class="col-12 col-lg-8 product-detail-info">
                <img src=<%= product.images %> alt="" class="product-detail-img">
                <p class="product-detail-description">
                    <%= product.descripcion_producto %>
                </p>
            </article>
            <div class="col-12 col-lg-4">
                <section class="comments">
                        <% for( let i = 0; i < 5; i++ ) { %>
                            <article>
                                <img src="<%= product.fotoPerfil[i] %>" alt="">
                                <div class="data">
                                    <p class="user">
                                    <strong> <a href="/profiles"><%= product.email[i] %></a> </strong>
                                    <%= product.comentarios[i] %></p>
                                </div>
                            </article>
                        <% } %> 

                </section>
                <form class="form" action="/products/detail/<%= product.id %>" method="get">
                    <input class="form-input" type="text" value="" placeholder="Agregar comentario">
                    <button type="submit" class="buy-now-button">AGREGAR COMENTARIO</button>
                </form>
                <hr>
            </div>
        </div>
    </div>
</div>
<!-- /product detail -->
<%- include('./partials/footer') %>
</body>
</html>